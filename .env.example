# =============================================================================
# Camera Monitoring System (MCS) - Environment Configuration
# =============================================================================
# Copy this file to .env and customize for your environment
# NEVER commit .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
POSTGRES_USER=mcs_user
POSTGRES_PASSWORD=mcs_password
POSTGRES_DB=mcs_db
POSTGRES_PORT=5432

# Database connection URL (for backend)
# Format: postgresql://user:password@host:port/database
# Note: Use 'postgres' as host (Docker service name), not 'localhost'
DATABASE_URL=postgresql://mcs_user:mcs_password@postgres:5432/mcs_db

# -----------------------------------------------------------------------------
# Backend Configuration
# -----------------------------------------------------------------------------
# Secret key for JWT token signing
# Generate with: openssl rand -hex 32
# IMPORTANT: Change this in production!
SECRET_KEY=dev-secret-key-change-in-production

# JWT algorithm
ALGORITHM=HS256

# Access token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Backend host and port
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------
# Frontend port (Next.js)
FRONTEND_PORT=3000

# API URL for browser requests
# IMPORTANT: Must use 'localhost' (not 'backend') for browser access
# This is exposed to the browser via NEXT_PUBLIC_* prefix
NEXT_PUBLIC_API_URL=http://localhost:8000

# Backend API URL (for server-side requests)
# Can use 'backend' (Docker service name) for internal requests
API_BASE_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# Environment Settings
# -----------------------------------------------------------------------------
# Environment: development, staging, production
ENVIRONMENT=development

# Debug mode: true, false, or WARN
DEBUG=true

# =============================================================================
# Port Configuration Notes
# =============================================================================
# All ports are configurable. If defaults conflict with other services:
#
# Example alternative ports:
# POSTGRES_PORT=5433
# BACKEND_PORT=8001
# FRONTEND_PORT=3001
# NEXT_PUBLIC_API_URL=http://localhost:8001
#
# See docs/port-configuration.md for details
# =============================================================================

# =============================================================================
# Security Notes
# =============================================================================
# 1. Change SECRET_KEY in production (use: openssl rand -hex 32)
# 2. Change POSTGRES_PASSWORD in production
# 3. Set DEBUG=false in production
# 4. Set ENVIRONMENT=production in production
# 5. Never commit .env file to version control
# =============================================================================
